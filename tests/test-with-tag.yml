name: Test with tag input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create release-drafter config
        run: |
          mkdir -p .github
          cat > .github/release-drafter.yml << 'EOF'
          name-template: 'v$RESOLVED_VERSION 🌈'
          tag-template: 'v$RESOLVED_VERSION'
          categories:
            - title: '🚀 Features'
              labels:
                - 'feature'
                - 'enhancement'
          change-template: '- $TITLE @$AUTHOR (#$NUMBER)'
          template: |
            ## Changes

            $CHANGES
          EOF

      - id: action
        uses: .
        with:
          tag: 'v9.9.9'
          dry-run: 'true'
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Assert tag_name output matches custom tag
        env:
          ACTUAL: ${{ steps.action.outputs.tag_name }}
        run: |
          echo "actual tag_name: $ACTUAL"
          if [ "$ACTUAL" != "v9.9.9" ]; then
            echo "FAIL: expected 'v9.9.9' but got '$ACTUAL'"
            exit 1
          fi
          echo "PASS: tag_name output matches custom tag"
